{
  "name": "jutsuper",
  "version": "1.0",
  "author": "kerdl",
  "dependencies": {
    "commander": "^12.0.0",
    "fs-extra": "^11.2.0",
    "glob": "^10.3.10"
  },
  "scripts": {
    "build-manifest2": "node build.mjs --manifest 2",
    "build-manifest3": "node build.mjs --manifest 3",
    "build": "node build.mjs --manifest all"
  },
  "devDependencies": {
    "c-preprocessor": "^0.2.13"
  }
}
